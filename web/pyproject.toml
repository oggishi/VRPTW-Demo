[project]
name = "vrptw-backend"
version = "0.1.0"
description = "VRPTW API backend"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.116.0",
  "uvicorn[standard]>=0.35.0",
  "httpx>=0.28.0",
  "pydantic>=2.11.0"
]

[tool.uv]
package = false
